{
  "name": "portfolio-platform",
  "version": "1.0.0",
  "description": "Portfolio Platform Root",
  "private": true,
  "main": "index.js",
  "scripts": {
    "install:backend": "cd apps/backend && npm install",
    "build:backend": "cd apps/backend && npm run build",
    "start": "node index.js",
    "postinstall": "npm run install:backend && npm run build:backend"
  }
}
